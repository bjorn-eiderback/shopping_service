apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-postgres
  namespace: shopping
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users-postgres
  template:
    metadata:
      labels:
        app: users-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16
          env:
            - name: POSTGRES_DB
              value: users
            - name: POSTGRES_USER
              value: users
            - name: POSTGRES_PASSWORD
              value: users
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: users-postgres
  namespace: shopping
spec:
  selector:
    app: users-postgres
  ports:
    - port: 5432
      targetPort: 5432
