apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-postgres
  namespace: shopping
spec:
  replicas: 1
  selector:
    matchLabels:
      app: audit-postgres
  template:
    metadata:
      labels:
        app: audit-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16
          env:
            - name: POSTGRES_DB
              value: audit
            - name: POSTGRES_USER
              value: audit
            - name: POSTGRES_PASSWORD
              value: audit
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: audit-postgres
  namespace: shopping
spec:
  selector:
    app: audit-postgres
  ports:
    - port: 5432
      targetPort: 5432
